# Описание реализации проекта "Интернет-магазин обуви"

> 🚧 **Черновик документа** - в процессе заполнения

## 📋 Содержание

1. [Обзор проекта](#-обзор-проекта)
2. [Технологический стек](#️-технологический-стек)
3. [Архитектура приложения](#️-архитектура-приложения)
4. [Структура проекта](#-структура-проекта)
5. [Основные компоненты](#-основные-компоненты)
6. [Управление состоянием](#️-управление-состоянием)
7. [Работа с API](#-работа-с-api)
8. [Маршрутизация](#️-маршрутизация)
9. [Особенности реализации](#-особенности-реализации)

---

## 🎯 Обзор проекта

**Интернет-магазин обуви** - дипломный проект курса React от Netology.

### Основной функционал:
- ✅ Просмотр каталога товаров
- ✅ Фильтрация по категориям
- ✅ Поиск товаров
- ✅ Просмотр деталей товара
- ✅ Добавление в корзину
- ✅ Оформление заказа

---

## 🛠️ Технологический стек

### Основные технологии:
- **React 19** - библиотека для построения UI
- **TypeScript** - типизация
- **Redux Toolkit** - управление состоянием
- **Redux Saga** - обработка побочных эффектов
- **React Router v7** - маршрутизация
- **Axios** - HTTP клиент
- **Vite** - сборщик проекта

### Дополнительные инструменты:
- **ESLint** - линтинг кода
- **CSS Modules** - стилизация компонентов

---

##  Архитектура приложения

### Общая структура

```plantext
┌─────────────────────────────────────────┐
│           main.tsx (Entry)              │
│  ┌───────────────────────────────────┐  │
│  │      Redux Provider               │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │    React Router             │  │  │
│  │  │  ┌───────────────────────┐  │  │  │
│  │  │  │   App Component       │  │  │  │
│  │  │  │  • Header             │  │  │  │
│  │  │  │  • Pages (Outlet)     │  │  │  │
│  │  │  │  • Footer             │  │  │  │
│  │  │  └───────────────────────┘  │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### Поток данных - ДОБАВИТЬ словесное описание

```plantext
User Action → Component → dispatch(action) → 

→ Saga → API → dispatch(success/failure) → 

→ Reducer → Store → Component → Re-render
```

---

## 📁 Структура проекта (frontend)

```plantext
online-shoe-store/
├── src/
│   ├── components/
│   │   ├── entities/          # Бизнес-сущности (TopSales, Catalog, Cart)
│   │   │   ├── TopSales/
│   │   │   │   ├── TopSales.tsx
│   │   │   │   ├── TopSales.module.css
│   │   │   │   └── index.ts
│   │   │   ├── Catalog/
│   │   │   ├── Cart/
│   │   │   ├── ProductCard/    # Generic компонент для карточки товара
│   │   │   └── Banner/         # Рекламный баннер
│   │   ├── pages/              # Страницы приложения
│   │   │   ├── MainPage/
│   │   │   ├── CatalogPage/
│   │   │   ├── ProductPage/
│   │   │   ├── CartPage/
│   │   │   ├── AboutPage/
│   │   │   ├── ContactsPage/
│   │   │   └── NotFoundPage/
│   │   ├── layouts/            # Макеты (Header, Footer)
│   │   │   ├── Header/
│   │   │   └── Footer/
│   │   └── shared/             # Переиспользуемые компоненты
│   │       ├── utils/          # Вспомогательные функции (debounce, formatters)
│   │       ├── widgets/        # Виджеты (SearchWidget, CartWidget)
│   │       │   ├── SearchWidget/
│   │       │   └── CartWidget/
│   │       └── ui/             # UI компоненты (Preloader, ErrorMessage)
│   │           ├── Preloader/
│   │           ├── ErrorMessage/
|   |
│   ├── models/                 # Модели данных и типы
│   │   ├── type.ts             # TypeScript интерфейсы (Item, Category, etc.)
│   │   └── react-node.d.ts     # Типы для React
│   ├── redux/                  # Глобальное состояние (Redux Toolkit + Saga)
│   │   ├── actions/            # Action creators
│   │   │   ├── topSalesActions.ts
│   │   │   ├── categoriesActions.
│   │   │   ├── catalogActions.ts
│   │   │   ├── itemActions.ts
│   │   │   ├── cartActions.ts
│   │   │   └── orderActions.ts
│   │   ├── reducers/           # Reducers (createSlice)
│   │   │   ├── topSalesSlice.ts
│   │   │   ├── categoriesSlice.ts
│   │   │   ├── catalogSlice.ts
│   │   │   ├── itemSlice.ts
│   │   │   ├── cartSlice.ts
│   │   │   └── orderSlice.ts
│   │   ├── sagas/              # Sagas для асинхронных операций
│   │   │   ├── topSalesSaga.ts
│   │   │   ├── categoriesSaga.ts
│   │   │   ├── catalogSaga.ts
│   │   │   ├── itemSaga.ts
│   │   │   ├── cartSaga.ts
│   │   │   ├── orderSaga.ts
│   │   │   └── rootSaga.ts
│   │   └── store/              # Настройка store
│   │       └── index.ts
│   ├── routes/                 # Маршрутизация
│   │   └── AppRoutes.tsx       # Определение маршрутов
│   ├── hooks/                  # Кастомные хуки
│   │   ├── useAppDispatch.ts   # Typed dispatch
│   │   ├── useAppSelector.ts   # Typed selector
│   │   ├── useDebounce.ts      # Debounce для поиска
│   │   └── useLocalStorage.ts  # Хук для localStorage
│   ├── services/               # Сервисы для работы с данными
│   │   ├── api/                # API сервис (Axios)
│   │   │   └── apiService.ts
│   │   └── cart/               # Сервис корзины (localStorage)
│   │       └── cartService.ts
│   ├── main.tsx               # Точка входа
│   ├── App.tsx                 # Корневой компонент
|   ├── App.css
│   ├── index.css                 # Глобальные стили
│   └── style.css               # Стили из HTML (временно, для кастомизации)
├── .env                        # Переменные окружения (API URL)
├── images.d.ts
├── public/                     # Статические файлы
│   ├── favicon.ico              # Фavicon
|   ├── images/                # Изображения
│   │   ├── banner.jpg            # Баннер
│   │   ├── products/           # Изображения товаров
│   │   ├── footer-sprite.png       # Иконка социальных сетей, карточек
│   │   ├── header-controls-sprite.png       # Иконка поиска, корзины, человека
│   │   ├── header-logo.png             # логоти
├── vite.config.ts              # Конфигурация Vite (aliases, plugins,...)
├── tsconfig.json               # TypeScript конфиг
├── cypress/                    # E2E тесты
│   ├── integration/
│   │   ├── main-page.cy.ts
│   │   ├── catalog-page.cy.ts
│   │   ├── product-page.cy.ts
│   │   └── cart-page.cy.ts
│   └── support/
│       └── commands.ts
└── package.json
```

---

## 🧩 Основные компоненты

### 1. Страницы (Pages)

#### MainPage
- Отображает баннер
- Показывает хиты продаж (TopSales)
- Отображает каталог с фильтрами

#### CatalogPage
- Поиск товаров
- Фильтрация по категориям
- Пагинация (загрузка по 6 товаров)

#### ProductPage
- Детальная информация о товаре
- Выбор размера
- Выбор количества
- Добавление в корзину

#### CartPage
- Список товаров в корзине
- Изменение количества
- Удаление товаров
- Форма оформления заказа


#### Catalog
- Отображение сетки товаров
- Интеграция с Redux
- Обработка состояний загрузки

#### Categories
- Список категорий
- Активная категория
- Фильтрация каталога

#### Cart
- Таблица товаров
- Расчет общей суммы
- Управление количеством
- Удаление товаров
- Оформление заказа


#### Header
- Логотип и навигация
- Виджет поиска
- Индикатор корзины

#### Footer
- Информация о магазине
- Ссылки на страницы

---

## 🗄️ Управление состоянием

### Redux Store

```typescript
store = {
  topSales: {
    items: Item[],
    loading: boolean,
    error: string | null
  },
  categories: {
    items: Category[],
    loading: boolean,
    error: string | null
  },
  catalog: {
    items: Item[],
    loading: boolean,
    error: string | null,
    categoryId: number | null,
    searchQuery: string,
    offset: number,
    hasMore: boolean
  },
  item: {
    item: Item | null,
    loading: boolean,
    error: string | null
  },
  cart: {
    items: CartItem[],
    loading: boolean,
    error: string | null
  }
}
```

### Основные Slices

- **topSalesSlice** - хиты продаж
- **categoriesSlice** - категории товаров
- **catalogSlice** - каталог товаров
- **itemSlice** - детали товара
- **cartSlice** - корзина покупок

### Redux Saga

Используется для:
- Асинхронных API запросов
- Обработки ошибок
- Сложной бизнес-логики

---

## 🌐 Работа с API

### API Endpoints

```typescript
// Хиты продаж
GET /api/top-sales

// Категории
GET /api/categories

// Каталог товаров
GET /api/items?categoryId=X&q=search&offset=N

// Детали товара
GET /api/items/:id

// Оформление заказа
POST /api/order
```

### API Service

```typescript
export const apiService = {
  getTopSales: () => Promise<Item[]>,
  getCategories: () => Promise<Category[]>,
  getItems: (params) => Promise<Item[]>,
  getItem: (id) => Promise<Item>,
  postOrder: (order) => Promise<void>
}
```

---

## 🗺️ Маршрутизация

### Конфигурация маршрутов

```typescript
routes = [
  { path: '/', element: <MainPage /> },

  { path: '/catalog', element: <CatalogPage /> },

  { path: '/product/:id', element: <ProductPage /> },

  { path: '/cart', element: <CartPage /> },

  { path: '/about', element: <AboutPage /> },

  { path: '/contacts', element: <ContactsPage /> },

  { path: '*', element: <NotFoundPage /> }
  
]
```

---

## ⚡ Особенности реализации

### 1. Управление корзиной

- Хранение в Redux Store
- Персистентность через localStorage
- Уникальность по паре (id, size)
- Автоматическое суммирование количества

### 2. Поиск товаров

- Виджет в Header
- Переход на CatalogPage с параметром q
- Сохранение поискового запроса в Redux

### 3. Фильтрация по категориям

- Динамическая загрузка категорий
- Сброс offset при смене категории
- Категория "Все" для показа всех товаров

### 4. Пагинация

- Загрузка по 6 товаров
- Кнопка "Загрузить ещё"
- Скрытие кнопки при отсутствии товаров

### 5. Обработка ошибок

- Отображение Preloader при загрузке
- Показ ErrorMessage при ошибках
- Возможность повторного запроса

### 6. Типизация

- Строгая типизация всех компонентов
- Типизация Redux state
- Типизация API ответов

---

## 🚀 Запуск проекта

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

## Планирование  развития проекта

Добавление автотестирования
